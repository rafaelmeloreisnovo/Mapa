# RAFAELIA Ω177 — R1 Local Closure Receipt — G074 + G094 + G097

Date: 2026-09-09
State: LOCAL_CONTRACTS_VALIDATED / POPULATION_NOT_CLOSED / CLAIM_ALLOWED=false

## Scope

This receipt closes only the locally executable contract/tool layer for:

- G074 — NUMERIC_SEMANTICS_PRECISION_POLICY
- G094 — PAPER_CODE_DATA_SYNC
- G097 — SUPERSESSION_RETENTION_POLICY

Invariant preserved:

`contract closed != population closed`

## Local validation

Validator:
`tools/validate_omega177_r1_contracts.py`

Contract:
`contracts/omega177/omega177_r1_local_contracts_v1.json`

Observed local result before materialization:

`PASS: G074 G094 G097 contracts valid; claim_allowed=false; R1->R2 gate valid`

## G074

Closed locally as a versioned policy contract:
- signed overflow forbidden;
- unsigned wrap only when modulo semantics are explicit;
- shift counts must satisfy `0 <= shift < bit_width`;
- narrowing conversions require explicit range checks;
- fixed-point requires storage/fraction bits, rounding and saturation metadata;
- floating comparisons require absolute and/or relative tolerance;
- NaN/Inf are rejected unless explicitly part of the domain;
- rationals use canonical reduced form with positive denominator and zero=`0/1`.

Population-level proof that every implementation obeys this policy remains OPEN.

## G094

Closed locally as a synchronization contract. A paper claim can be `BOUND` only with exact non-empty bindings to:
- claim_id;
- paper_ref;
- code_ref;
- data_ref;
- receipt_ref;
- version_or_hash;
- sync_state.

Any mismatch routes to `CLAIM_BLOCKED`.

Population-level binding across the complete paper/code/data corpus remains OPEN.

## G097

Closed locally as an append-only supersession/retention contract:
- immutable history;
- explicit predecessor→successor edges;
- supersession reason/time/authority required;
- no destructive deletion of SOURCE/EVIDENCE/RECEIPT;
- retirement by tombstone or supersession edge;
- deduplicated views may collapse duplicates while physical history remains preserved.

Population-level supersession graph over all snapshots remains OPEN.

## R1 consolidation

R1 local contract layer is now ready to consolidate.
Promotion rule remains:

`contract closed != population closed`

## R2 entry gate

R2 focus: Corpus + custody.
Promotion rule:

`bytes observed before cryptographic root`

R2 may begin with inventory/dedup/provenance/root construction, but must not claim canonical global closure before the complete observed byte population and authority bindings are present.

## External gaps intentionally not closed

Remain external/real:
- Android/ARM physical execution;
- second physical architecture;
- RafBit↔BitOmega semantic authority;
- independent external audit;
- empirical G017/G018/RLL measurements;
- provider/external trust anchors and other external systems.

No external gap is promoted by this receipt.
