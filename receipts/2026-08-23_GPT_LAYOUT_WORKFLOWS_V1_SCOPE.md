# GPT Layout Workflows V1 Scope Boundary

This artifact records the claim boundary for the GPT layout delta.

```text
repository_contract = IMPLEMENTED_PROPOSED
runtime_behavior     = TOKEN_VAZIO_NOT_PROVEN
claim_allowed        = false
```

The repository can validate pointers, layer order, workflow profiles,
fail-closed fallbacks and required invariants.

It cannot certify hidden model state, hidden chain of thought,
or universal future ChatGPT behavior.

Promotion beyond `REFERENCE` requires evidence applicable to the claim.
