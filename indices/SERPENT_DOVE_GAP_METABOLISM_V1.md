# Serpent–Dove Gap Metabolism V1 — ATLAS/NOVO/Ω Route

Status: `IMPLEMENTED_PENDING_REMOTE_CI`  
Policy: `APPEND_ONLY | FAIL_CLOSED | MINIMUM_SUFFICIENT_INTERVENTION | claim_allowed=false`  
Base authority: `rafaelmeloreisnovo/Mapa@8e2f3933e5d49cab7e36a1bbc4acbb8a4116952e`

## Purpose

Materialize the previously nominal `SERPENT_DOVE_CONDUCT` bridge as an executable governance contract and connect it to live gap metabolism across Mapa, NOVOexport custody/semantics, and the RLL primordial producer route.

This is an extension. It does **not** rewrite or replace Omega Assurance Mesh V1, the attention registry, authority pyramid, operational ontology, custody history, or producer authority.

## Routing macros

```text
ATLAS:X = choose the evidence/authority route before acting
NOVO:X  = inspect NOVOexport custody/JSON-derived evidence first when the claim originates there
L:X     = append predecessor -> successor transitions; do not rewrite history
O:X     = require independent evidence axes where independence matters
T:X     = map cross-layer/cross-repository consequences
REL:X   = type relations; relation != authority fusion
SCALE:X = project META -> ecosystem -> repository -> route -> object -> record/token
EVID:X  = evidence, falsifier, gate and receipt only
GAP:X   = preserve unknowns as typed TOKEN_VAZIO/attention states
LEARN:X = append closed learning with provenance and boundary
```

## Serpent–Dove boundary

Matthew 10:16 is recorded as the provenance of the metaphor, with epistemic type `PARABLE` and engineering evidence weight `0`. It is not technical proof, scientific validation, legal authority, or execution permission.

The engineering heuristic is deliberately bounded:

```text
Omega_SP = Discernment * NonHarm * Reversibility
status = ARCHITECTURAL_HEURISTIC_NOT_VALIDATED_METRIC
```

Its operational meaning is qualitative: inspect deeply; claim only what evidence supports; intervene only within authority and the smallest sufficient reversible scope.

## Materialized contracts

- `schemas/omega-assurance/serpent-dove-transition.v1.schema.json`
- `data/control-plane/omega-assurance/serpent-dove-conduct.v1.json`
- `data/control-plane/omega-assurance/gap-metabolism-registry.v1.json`
- `data/fixtures/omega-assurance/serpent-dove-negative-cases.v1.json`
- `tools/validate_serpent_dove_gap_metabolism_v1.py`
- `tests/test_serpent_dove_gap_metabolism_v1.py`
- `.github/workflows/serpent-dove-gap-metabolism-v1.yml`

## Eight gap operators

Every active gap is forced through all eight projections:

| Operator | Required question |
|---|---|
| DIRECT | What is directly observed at the exact ref/provider? |
| INVERSE | What counter-observation would overturn the interpretation? |
| REVERSIVE | How can intervention or prior state be safely reconstructed? |
| DERIVATIVE | What changed versus the predecessor and on which axis? |
| ANTIDERIVATIVE | Which lineage and boundary conditions reconstruct the current state? |
| ORTHOGONAL | Which genuinely independent evidence axis can test it? |
| TRANSVERSE | What other routes, producers, risks or claims are affected? |
| SCALE | How does it project from META to the smallest meaningful unit? |

## Current gap census

The V1 registry contains **17 active gaps** plus **2 append-only closed learnings**. This is a bounded census of the inspected surfaces, not an ecosystem-exhaustiveness claim.

### P0 — non-compensatory HOLD

1. Mapa server-side merge/check enforcement not demonstrated at the observed `main` provider state.
2. Independent review/promotion authority for this new wave is unresolved until the exact-head PR receives such review.
3. RLL full primordial verdict remains `TOKEN_VAZIO`; scoped B/P/BBN/CMB progress cannot compensate unresolved perturbation/raw-likelihood/MCMC prerequisites.

### P1 — causal blockers / highest information gain

- RAW018 current hard custody.
- stale/broken NOVO route pointers.
- exact 19-shard architecture pipeline full run.
- message/node full projection.
- logical-unit/dedup total.
- token-occurrence off-by-one root cause.
- current-head Mapa ontology/federation revalidation.
- first producer CrossFail execution.
- RLL B/P perturbation microphysics.
- RLL raw BBN+CMB replay.
- RLL post-RNG converged MCMC reference.

### P2 — important bounded debt

- structural orphan review (17,037 record-level structural signals in the fast cross-check; not 17,037 unique architectures).
- exact data -> chunk lineage.
- authoritative RLL B/P statistical prior and prior-sensitivity.

## NOVOexport evidence boundary

The 2026-08-26 private receipt observed the bound MESSAGES series as 19/19 shards, 91,232 parseable records and zero parse errors in that selection. It also observed 7,821 records with unresolved/TOKEN_VAZIO vocabulary and 17,037 records with structural signals without a named canonical family.

Those counts are **fast semantic cross-check evidence**, not the exact end-to-end output of `scripts/discover_architecture_families.py`. The exact run remains a P1 gap. Private Drive locators are deliberately absent from this public bundle.

## Current-state correction learned from RLL

The prior session state `PR772 CI pending` is now superseded for that narrow governance fact: PR #772 is merged at its exact producer head, and the retrieved exact-head check set contains successful checks plus conditional skipped work; no retrieved check had failure/cancelled/timed_out/neutral conclusion.

This closes only the stale CI-pending statement. It does **not** close:

- B/P perturbation microphysics;
- raw BBN abundance likelihood replay;
- raw ACT spectra/CAMB replay;
- post-RNG converged MCMC reference;
- full primordial RLL verdict.

`merge != scientific confirmation` and `skipped != executed` are executable negative fixtures in this V1.

## Quiet Watchdog

```text
WATCH
  -> weak/ambiguous signal -> CORRELATE -> WATCH
  -> sufficient bounded evidence -> WARN
  -> unknown authority/impact/reversibility -> HOLD
  -> known authority + bounded evidence + reversible scope -> ACT_BOUNDED
```

`META_WATCH_DEPTH=2`; overflow is `HOLD_FOR_AUDIT`, preventing infinite watchdog recursion.

## Anti-regression suite

The dedicated workflow compiles and runs the new validator/tests, then re-runs:

- Omega Assurance Mesh V1 unit tests and validator;
- Omega Assurance Skills V1 validator;
- Adaptive Resilience Watchdog V1 validator;
- Authority Pyramid self-test.

Negative intervention fixtures reject, among other errors:

- search miss -> ABSENT;
- provider NOT_FOUND -> DELETED;
- ambiguous signal -> ACT_BOUNDED;
- unknown authority/rollback -> ACT_BOUNDED;
- historical evidence -> CURRENT_PASS;
- skipped check -> EXECUTED;
- parable -> ENGINEERING_PROOF;
- fast aggregate -> EXACT_PIPELINE_PASS;
- PR merge -> SCIENTIFIC_CONFIRMATION;
- mitigation success -> ROOT_CAUSE_RESOLVED;
- private locator -> PUBLIC_PAYLOAD.

Positive controls verify that ambiguous evidence may remain in WATCH and that evidenced, authorized, reversible action may be ACT_BOUNDED.

## F_ok / F_gap / F_next

`F_ok`: the missing dedicated conduct layer, typed gap metabolism, eight operator projections, urgency/no-compensation rule, negative-overreaction suite, privacy boundary and cross-producer learning are materialized on an isolated branch.

`F_gap`: remote CI for the exact new head, independent review, server-side enforcement, exact NOVO full pipeline/current custody closures, producer CrossFail execution, and remaining RLL scientific gates are unresolved until their named evidence appears.

`F_next`: open a draft PR; observe exact-head CI; fix any structural regression without weakening fail-closed tests; append a qualification receipt; then execute the highest-value P1 probe under the owning producer/Drive authority.
