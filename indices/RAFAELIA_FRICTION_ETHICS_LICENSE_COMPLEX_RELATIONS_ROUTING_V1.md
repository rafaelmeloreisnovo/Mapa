# RAFAELIA — Friction / Ethics / License / Complex Relations Routing V1

Date: 2026-08-22  
State: `ROUTING_CANDIDATE / APPEND_ONLY / claim_allowed=false`

## Function

This file is a Mapa routing/index object. It does not copy RafGitTools
governance logic and does not copy GAIA producer implementation.

```text
objective
→ authority
→ provenance
→ uncertainty
→ friction class
→ providence
→ test/falsifier
→ receipt
→ delta
→ index
```

## Authority map

| Domain | Authority | Role |
|---|---|---|
| friction/uncertainty/rights/ethics governance | `rafaelmeloreisnovo/RafGitTools` | contract, validators, use/license/relation ledgers |
| complex feedback implementation | `rafaelmeloreisnovo/GAIA_phi` | producer code/tests/runtime artifacts |
| federated routing/state | `rafaelmeloreisnovo/Mapa` | pointers, gaps, route state, receipts |
| longitudinal editorial memory | Google Drive | reconstruction/checkpoints, not runtime proof |
| scientific/formal claims | domain producer/source | remains separate from routing/governance |

No green state in one authority silently promotes another authority.

## RafGitTools V3 candidate

- PR: `rafaelmeloreisnovo/RafGitTools#377`
- branch: `governance/friction-reduction-ethics-license-v3-20260822`
- observed head: `a17dbca5c1a4e6222575221dafaae75e38164406`
- state at route creation: `OPEN / DRAFT / mergeable`
- base: `main@8e78ea64573b95c5edaa93ea94627f248c2c1e80`
- `claim_allowed=false`

Core V3 artifacts:

```text
configs/uncertainty-urgency-friction-ethics-license.v3.json
data/governance/license-friction-matrix.v1.json
data/governance/ethics-by-design-use-registry.v1.json
data/governance/complex-network-design-relations.v1.json
data/governance/friction-baseline.v1.json
data/evidence/github/gaia-provider-execution-friction-20260822.v1.json
scripts/validate_uncertainty_urgency_friction_v3.py
scripts/validate_friction_baseline_v1.py
tests/test_uncertainty_urgency_friction_v3.py
tests/test_friction_baseline_v1.py
.github/workflows/uncertainty-urgency-friction-v3.yml
```

## Producer anchor — GAIA complex feedback

```text
repo  = rafaelmeloreisnovo/GAIA_phi
commit = d3f49c10b74f740ee2024314dff91e9a0ef20b2f
blob   = 1bb228392c51d3bfd0d85ae8e6ce92c40fe28dd1
path   = dados/cognitive_symbiotic.py
PR98   = merged at f980cc5be46b8daff892e3885da40097eba46e90
```

Observed implementation contains contextual weighted contraction, directional
projection, temporal difference, complex phase modulation, local standard
normalization and gated complex-to-real residual feedback.

This is an implementation observation, not a scientific cognition claim.

## Provider execution friction

The first GAIA `cognitive-symbiotic-receipt` attempt and its explicit rerun both
ended `failure` with zero exposed job steps in the connector view.

```text
attempt 1 job = 97011790745
attempt 2 job = 97033065513
run = 32564704743
observed test steps = 0
state = TOKEN_VAZIO_PROVIDER_EXECUTION
```

Therefore:

```text
workflow failure != executed test failure
provider-friction candidate != proven provider root cause
merged implementation != execution receipt
```

The failed predecessor and retry are both preserved.

## License friction route

Current bounded rights units:

1. GAIA repository source — observed MIT, source-only scope;
2. PyTorch dependency — separate conditional rights unit;
3. model weights — `TOKEN_VAZIO_LICENSE` unless bound;
4. datasets/external payloads — `TOKEN_VAZIO_LICENSE` unless bound.

A repository license never silently covers dependencies, weights, datasets or
third-party payloads.

## Ethics-by-Design usage route

Five bounded uses are materialized in RafGitTools:

- deterministic GAIA synthetic fixture;
- license classification;
- parable internal navigation;
- Mapa routing pointer;
- Drive longitudinal editorial mirror.

Each use binds purpose, necessity, minimization, risk, review, rights, privacy,
rollback/boundary, provenance and evidence state.

## Complex relations by design

The bounded RafGitTools registry currently declares 14 nodes and 11 typed
relations. Every evidence-bearing edge requires source, authority domain,
uncertainty, evidence effect, boundary/falsifier and next gate.

```text
analogy -> evidence_effect NONE
co-occurrence -> evidence_effect NONE
visual proximity -> no evidence promotion
similarity -> no equivalence promotion
```

`B7_TO_T2_BRIDGE` remains `TOKEN_VAZIO_BRIDGE`.

## Friction baseline route

Current bounded component vector:

| Component | Current value |
|---|---:|
| unresolved gaps | 5 |
| unbound license units | 2 |
| provider pre-step failure attempts | 2 |
| untyped bounded relations | 0 |
| governed uses missing rollback | 0 |
| governed uses/relations missing provenance field | 0 |
| manual rework | `TOKEN_VAZIO_UNMEASURED` |

Because one component is unmeasured and component semantics differ:

```text
aggregate = TOKEN_VAZIO_SCORE
```

No scalar truth/quality score is inferred.

## Parable internal route

```text
TOKEN_VAZIO -> tijolo ausente identificado
residual -> fissura que aponta onde medir
falsifier -> esquadro que pode reprovar a parede
license -> escritura dos usos permitidos
receipt -> assinatura observável da etapa
rollback -> retorno ao último ponto seguro
index -> mapa da obra
contract -> planta com limites e aceitação
```

`parable_evidence_effect=NONE`.

The phrase `nenhum limite é real, pois o código executa em verbo vivo` remains
`PARABLE_ASPIRATION`. Hardware, mathematics, license, safety, privacy, evidence,
authority and runtime remain binding operational limits.

## Reconstructible symbolic route

```text
STATISTICS
→ TOKENS
→ METAPHORS
→ VECTORS / WORDS
→ PROMISE / CONTRACT
→ EXECUTION
→ RECEIPT
→ DELTA
→ Ω^n
```

`Ω^n` is a reconstruction/coherence marker, not a scientific proof state.

## Urgency routing

| Gap | Priority | State |
|---|---|---|
| `TV-V3-PROVIDER-CLASS-001` | P0 | `TOKEN_VAZIO_PROVIDER_EXECUTION` |
| `TV-V3-LICENSE-PAYLOAD-001` | P0 | `PARTIAL / residual license inventory open` |
| `TV-V3-USE-REGISTRY-001` | P0 | `CLOSED_MATERIALIZED_LIMITED` |
| `TV-V3-COMPLEX-RELATIONS-001` | P1 | `PARTIAL_BOUNDED_INVENTORY` |
| `TV-V3-FRICTION-BASELINE-001` | P1 | `PARTIAL_BASELINE` |
| `TV-V3-B7-T2-001` | P1 | `TOKEN_VAZIO_BRIDGE` |

Closing a token requires its exact receipt. A renamed token is not a closed token.

## F_ok / F_gap / F_next

`F_ok`

- V3 candidate has a bounded authority-preserving route.
- license, use, relation, provider and friction states are separately navigable.
- negative/provider evidence is preserved rather than hidden.
- parables are internally navigable without becoming evidence.

`F_gap`

- RafGitTools PR #377 is still a draft candidate at this checkpoint.
- V3 CI receipt is not yet bound in this routing record.
- GAIA remote execution remains unresolved after two zero-step failures.
- additional rights units remain fail-closed until they enter a real use.
- complex graph coverage is bounded, not exhaustive.
- B7→T² remains unresolved.

`F_next`

1. bind RafGitTools V3 CI outcome/artifact;
2. create a minimal provider-capability probe before blaming producer code;
3. append Drive checkpoint with exact PR/head/gate receipts;
4. update route by successor receipt, not by rewriting history;
5. close only evidence-backed residual tokens.
