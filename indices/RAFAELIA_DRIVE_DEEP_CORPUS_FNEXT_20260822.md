# F_next — Drive Deep Corpus — 2026-08-22

`claim_allowed=false`

Highest-value next action after A2 closure:

1. `A1 RAW018`: do not repeat identical filename searches. Probe only new evidence channels: archive member manifests, backup inventories, immutable byte witnesses, crosswalks, or a newly appearing provider route.
2. In parallel, continue `TV-DEEP-026/032` with parent-aware paginated census; every search/list miss remains non-terminal unless the provider returns terminal pagination evidence.
3. Prioritize archive lineage (`TV-DEEP-028/029/033`) because it can both expand corpus coverage and expose an alternate immutable RAW018 witness.
4. Execute `A3 FULL_CORPUS_OFFICIAL_V2` only after A1 is cryptographically/custodially recovered, unless an explicit governance amendment narrows the canonical corpus scope.
5. Treat llamaRafaelia PR113 as a separate post-merge review/runtime evidence problem; it does not block catalog identity finality.
