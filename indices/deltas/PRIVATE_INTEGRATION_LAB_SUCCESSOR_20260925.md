# Private Integration Lab — Federation Successor — 2026-09-25

**Mapa state:** `PUBLIC_SAFE_PROJECTION`  
**claim_allowed:** false  
**private payload copied:** no

## Authority correction

The current federation should read the repositories as:

```text
Mapa
  = federation map / authority routing / public-safe reconciliation

Rafaelia_Private
  = restricted evidence vault
  + private cross-repository integration laboratory

Producer repository
  = implementation/build/runtime/license authority for its own domain

RLL
  = scientific data/falsification/claim authority
```

The Private integration-lab role does not make Private the source of truth for
Vectra, llamaRafaelia, ZIPRAF_OMEGA_FULL, GAIA, RAFCODEΦ, RLL or another
producer.

## Current private succession

```text
PR #245
  -> merged governance/licensing/privacy baseline
  -> merge commit 6fa6685381fc7fc28b17999036798e10616570ff

PR #247
  -> active draft technical/evidence successor
  -> observed head a6004375e4da2447313a5cad204bb3cc21922bfc
```

Public Mapa stores no source payload from the private branch.

## Public-safe technical facts

Current bounded metadata says that the successor lab contains:

- typed source/delta/receipt contracts;
- a bounded deterministic context-capsule experiment (`RAFCAP1`);
- reference-only producer adapters;
- semantic-difference records;
- normalized evidence states;
- explicit license-boundary routing.

Current evidence boundary:

```text
RAFCAP1 host proof        = PASS (bounded local receipt)
ARMv7 cross-build         = PASS (bounded local receipt)
physical ARM execution    = TOKEN_VAZIO
GitHub hosted CI          = TOKEN_VAZIO / startup evidence
producer-native adapters  = NOT_RUN
cross-repo E2E promotion  = NOT_RUN
```

## Integration rule

```text
repo@commit:path
  + provenance
  + license
  + delta
  + adapter
  + test
  + receipt
  + rollback
```

A Private-lab result is eligible only for a **promotion proposal** to the
producer. The producer must establish its own resulting implementation/runtime
state.

## License invariant

The private v1000 policy is not projected as a global ecosystem license.

```text
GPL remains GPL
MIT historical grants remain granted
per-package licenses remain per-package
custom/ambiguous grants remain review-gated
v1000 applies only to explicitly scoped material that the rightsholder may license
```

## Privacy invariant

The public map may carry identifiers, states, commit/receipt hashes and bounded
results where authorized. It must not carry restricted source, credentials,
personal data, private research payloads or private evidence merely to make the
federation easier to navigate.

## R3

`F_ok`:
public/private authority boundary and successor routing are explicit.

`F_gap`:
producer-native executions, physical-device receipts and file-level licensing
remain open.

`F_next`:
consume the merged successor state only after its producer receipt/PR state is
updated; never infer private payload from public metadata.
