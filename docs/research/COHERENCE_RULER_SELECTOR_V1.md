# Coherence Ruler Selector V1

Status: `FORMAL / FAIL_CLOSED / claim_allowed=false`

The selector stops broad permutation search after a governed invariant identifies the smallest justified calculation region.

Core rule: `find ruler -> restrict region -> calculate only necessary paths -> emit receipt`.

It does not enumerate random total permutations after the ruler is found.
