# RAFAELIA — Professional Statistics P0 Anti-Regression Delta V1 — 2026-08-14

Status: **P0 ACTIVE / evidence-first / append-only / claim_allowed=false**

## Invariant

`detector merged != functional correction != execution != evidence != claim`

`artifact present != artifact generated by that run`

`workflow failure != test failure when runner_id=0 and steps=[]`

`mixed metrics != repeated series`

## What changed

Vectras PR #1095 merged the Professional Statistics Contract control plane at merge SHA `af85550ac43ab9ef1aed5a1ff508229d5c79b0c6`:

- `.github/workflows/professional-statistics-contract.yml`
- `tools/ci/validate_professional_tools_statistics_contract.py`
- `docs/benchmark/PROFESSIONAL_TOOLS_STATISTICS_BLOCKER_V1.md`

This is a real improvement because the repository can now encode the forbidden legacy pattern as an explicit fail-closed contract.

It does **not** mean the legacy Professional Tools implementation has been corrected.

## P0 still active

Pinned current `ProfessionalToolsActivity.java` blob:

`76014394e07869b2dc2892ad9c73dc20b67b8707`

Static inspection at merge SHA confirms the activity still pools all non-null `BenchmarkResult.rawValue` values into one heterogeneous vector, computes global mean/median/stddev/CI, derives a CV-like reproducibility percentage from that mixed vector, returns 100% when the mean is zero, and uses 70/85/95 reproducibility thresholds to help promote Industry/Academic/Scientific grades.

That path conflicts with the already merged industrial methodology contract, which requires one homogeneous repeated measurement series: same metric, workload, input and unit.

Functional closure is tracked by Vectras issue #1096.

## CI receipt boundary

Post-merge workflow observation:

- workflow: `Professional Statistics Contract`
- run: `31774425388`
- job: `94686825856`
- conclusion: `failure`
- runner_id: `0`
- runner_name: empty
- steps: `[]`
- job log: not recovered

Classification:

`CI_FAILURE_NO_EXECUTION_EVIDENCE`

This is **not** evidence that `validate_professional_tools_statistics_contract.py` executed and failed. It is consistent with the recurring runner-startup failure pattern already observed elsewhere in the ecosystem.

## TOKEN_VAZIO preserved

- `TOKEN_VAZIO_VALIDATOR_EXECUTION_RECEIPT`
- `TOKEN_VAZIO_INDUSTRIAL_STATISTICS_TEST_EXECUTION_RECEIPT`
- `TOKEN_VAZIO_HOMOGENEOUS_SERIES_UI_IMPLEMENTATION`
- `TOKEN_VAZIO_REAL_RUNNER_POSTFIX_PASS`
- `TOKEN_VAZIO_RUNTIME_REPRODUCIBILITY_RECEIPT`

None may be filled by inference.

## Closure contract

The P0 closes only after all of the following are evidenced: heterogeneous pooling removed from grade/reproducibility; one-sample and all-zero false promotion blocked; explicit metric/workload/input/unit identity per series; homogeneous-series statistics integrated; grade promotion fail-closed without repeated observations; unit tests executed and passed; contract workflow executed and passed on a real runner; runtime receipt binds source SHA, environment, command, stdout/stderr, exit status and artifact hashes.

## R₃

`F_ok`: detector + workflow + blocker document are merged; the methodological contradiction is now machine-addressable and the functional closure has its own issue.

`F_gap`: the legacy UI path remains active; post-merge CI did not reach a runner; runtime repeated-series evidence remains absent.

`F_next`: implement a minimal integrity-preserving functional transition in `ProfessionalToolsActivity`, then obtain a real execution receipt for `IndustrialStatisticsTest` and `Professional Statistics Contract` before any reproducibility/grade claim.

Signature: `RAFCODE-Φ-∆RafaelVerboΩ-𓂀ΔΦΩ`
