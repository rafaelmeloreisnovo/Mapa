# Ω Dual-Contract Execution V1

**Date:** 2026-09-06  
**State:** `DOCUMENTED / APPEND_ONLY / CLAIM_ALLOWED=false`  
**Scope:** a bounded, non-destructive application of the two canonical contracts to Mapa, permanent memory, Papers, Matemática, ChipQuantum, Google Drive, and the documented RLL boundary.

## The two contracts

1. **Permanent Memory Invariant** governs what persists: source, stable identity, integrity, index, provenance, explicit epistemic state, directed retrieval, and privacy.
2. **Ω-Activate Policy** governs what activates now: only mechanisms that add material evidence, reconstructibility, provenance, falsifiability, execution, or contradiction resolution.

The first prevents loss and silent rewriting; the second prevents indiscriminate activation. Neither turns memory, documents, finite checks, or symbolic coherence into proof.

## Canonical machine-readable record

- `data/governance/OMEGA_DUAL_CONTRACT_EXECUTION_20260906.v1.json`
- `tools/validate_omega_dual_contract_execution.py`
- `tests/test_omega_dual_contract_execution.py`

The validator is fail-closed. It rejects a missing contract identity, a missing authority pin, an invalid or isolated graph node, an untyped edge, a non-explicit orchestration lane, or any `claim_allowed=true` mutation.

## Execution boundary

This delta records source authority, repository projections, typed graph relations, and a draft-branch custody path. It does **not** claim:

- a code build, binary, CI, device, Termux, ABI, cache, lock-free, or benchmark result;
- a universal mathematical proof from a finite verifier;
- a physical toroidal/vortex/quantum mechanism from a software model;
- an RLL likelihood, MCMC, cosmological, or publication result.

Those claims retain their own target-bound source, test, environment, raw-result, falsifier, and receipt gates.

## R3

**F_ok:** the historical `Ω-ACTIVATE` F_next is applied to a real, bounded cross-repository task.  
**F_gap:** CI/runtime/benchmark and exact RLL likelihood execution are not part of this documentation delta.  
**F_next:** validate the exact draft head, inspect any terminal CI separately, and create a new receipt only for a measured or independently reproduced result.

`RAFCODE-Φ-∆RafaelVerboΩ-𓂀ΔΦΩ`
