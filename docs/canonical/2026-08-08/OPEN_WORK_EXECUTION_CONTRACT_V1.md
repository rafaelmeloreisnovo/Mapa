# RAFAELIA — Open Work Execution Contract V1

Date: 2026-08-08
State: DRAFT_REVIEW / claim_allowed=false / automatic_merge=false

## Purpose

Prevent open work from becoming anonymous, forgotten, implicitly discarded, or falsely closed. This document does not create scientific capability. It binds every authoritative open residual in the current RLL release ledger to an execution rule.

## Ruler

For every open token:

`source -> authority state -> invariant -> minimal evidence -> promotion condition -> falsifier -> next producer`

A token can remain open without becoming ambiguous. A successful draft CI run is evidence, not authoritative closure by itself.

## Current denominator

The governed residual set contains **14 authoritative open tokens**:

- P0: 4
- P1: 7
- P2: 3

Authority classes are deliberately separated: INTERNAL, MIXED, EXTERNAL, HUMAN and GOVERNANCE.

Two items currently carry successful draft evidence but remain authoritatively open until branch/ledger reconciliation:

1. `TOKEN_VAZIO_ACT_DR6_CMBONLY_MATERIALIZATION_REPRODUCTION` — RLL PR #681, workflow run `31238546026` PASS, artifact `9016264501`.
2. `TOKEN_VAZIO_H0_RD_FULL_BOLTZMANN_REPRODUCTION` — RLL PR #682, CLASS/CAMB runs `31238577630` and `31238577650` PASS, artifact `9016272335`; cross-engine r_drag agreement is ~3.4e-5 relative and the current lightweight approximation differs by ~0.6% at the tested vector.

Neither is promoted by this document.

## Priority order

### P0 — authority and scientific composition

1. Joint multi-probe Bayes: blocked until component gates and overlap/covariance policy close.
2. Independent replication: requires a genuinely independent authority; self-replay cannot close it.
3. Official DESI DR2 joint/cross-block: requires official immutable products and baseline reproduction.
4. Physical execution: requires actual Android/Termux evidence; hosted CI cannot substitute it.

### P1 — executable science and governance

- ACT DR6 CMB-only materialization: successful draft candidate for governed reconciliation; posterior-chain reproduction remains successor.
- DES Y6 3x2pt: official likelihood, scale cuts, nuisance contract and LCDM baseline remain external.
- RLL perturbation closure: theory contract is a prerequisite to solver code.
- RLL CLASS/CAMB implementation: blocked on the same frozen perturbation closure and must be independently implemented in the two engines.
- H0 primary-source custody: Planck/SH0ES exact values and overlap semantics remain source-authority work.
- H0/r_d full Boltzmann: standard CLASS/CAMB r_drag is cross-checked; formal inference still uses the lightweight approximation and must be replaced/replayed before closure.
- GitHub external settings: server-side rules/settings need direct platform receipts; YAML cannot prove them.

### P2 — bounded backlog and empirical runtime

- classify/retire all relevant 582 refs without shrinking the denominator invisibly;
- execute UTM-185 on intended physical Termux runtime;
- train/evaluate UTM-185 against a declared baseline and held-out protocol.

## Hard invariants

- `TOKEN_VAZIO != zero`.
- Draft PASS != merge != scientific promotion.
- External/human/physical authority cannot be synthesized by internal CI.
- Availability != reproduction.
- Standard LCDM/CPL engine agreement != RLL perturbation validity.
- Negative/null evidence remains first-class evidence.
- No tolerance may be relaxed after observing a failure without creating a new explicitly versioned hypothesis.

## F_ok

The 14 open residuals now each have authority, minimal proof, promotion condition, falsifier and next producer. The two active internal gaps have successful receipts but remain marked IN_FLIGHT rather than prematurely closed.

## F_gap

External likelihoods, primary-source custody, independent reproduction and physical-device execution remain dependent on their real authorities. RLL perturbations remain blocked on an explicit closure theory. Full-Boltzmann r_d is not yet integrated into the formal H0/r_d inference route.

## F_next

1. validate this contract in CI;
2. review/reconcile PRs #681 and #682 into the authoritative RLL ledger without overclaim;
3. execute internal P2 classification/training work;
4. collect official/external/physical authorities in parallel;
5. only construct joint multi-probe evidence after component and overlap gates close.
