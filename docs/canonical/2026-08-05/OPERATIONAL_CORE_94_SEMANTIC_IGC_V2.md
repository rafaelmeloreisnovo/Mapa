# Operational Core 94 — Evolução Semântica, Amplitude de Caminhos e IGC — V2

**Evento:** `OPCORE94-SEMIGC-20260805T2000-0300`  
**Modo:** `APPEND_ONLY / NON_DESTRUCTIVE / FAIL_CLOSED / EVIDENCE_FIRST`  
**Claim:** `claim_allowed=false`

## 1. Evolução sem apagar a fronteira

A contagem herdada permanece:

```text
expected_total = 94
identified     = 16
unitemized     = 78
```

A busca no Drive e no GitHub não localizou o artefato que individualiza originalmente os 94. Os IDs `OC94-017..OC94-094` continuam `TOKEN_VAZIO_ITEMIZATION_PENDING`.

## 2. Modelagem combinada

O registro deixou de ser somente uma lista e passou a conter um grafo tipado:

```text
fonte → estrutura → transformação → execução → evidência
      → interpretação → navegação → governança
```

Cada aresta percorre as sete direções:

```text
FATO → LACUNA → INVARIANTE → VARIANTE → PROVA/FALSIFICADOR → PARÁBOLA → RETROALIMENTAÇÃO
```

O ciclo evolutivo é:

```text
ψ intenção → χ observação → ρ ruído → Δ transmutação ética
→ Σ memória coerente → Ω fechamento honesto → ψ
```

## 3. Invariante geométrica coerente aplicada ao grafo

A transformação usada aqui é `DISCRETE_TYPED_GRAPH_PROJECTION`. Ela preserva somente:

- identidade do nó de origem;
- identidade do nó de destino;
- incidência tipada;
- estado epistemológico;
- bloqueio de claim.

Não preserva automaticamente distância, ângulo, causalidade ou equivalência científica.

A coesão real exige:

```text
G_S fonte/autoria
∧ G_D objeto/representação
∧ G_T transformação declarada
∧ G_I invariante compatível
∧ G_E evidência/teste
∧ G_F falsificador/caso negativo
∧ G_C custódia/versão/ambiente/limite
```

## 4. Resultado do delta

| Medida | Resultado |
|---|---:|
| Nós identificados | 16 |
| Caminhos tipados declarados | 37 |
| Caminhos `VERIFIED_LIMITED_STATIC` | 37 |
| Anchors `rmrCti` lidos integralmente | 6/6 |
| Outputs com provider ID + SHA-256 | 7/7 |
| Outputs com gerador em revisão fixa | 6/7 |
| Execução Termux física | 0 |
| Validação científica independente | 0 |

A amplitude útil nesta fase é:

\[
A_{útil,estática}=37/37
\]

Isso mede cobertura de custódia do grafo estático, não qualidade científica nem funcionamento físico.

## 5. Anchors `rmrCti`

A leitura fechou a camada documental:

1. árvore/grafo de conhecimento e proveniência;
2. integral Delta-P e nulo hipergeométrico exato;
3. contrato espectral Hyperformas-42;
4. articulações Geometric–Vetor–Verbo;
5. manifesto determinístico PMEP;
6. árvore CLI/TUI Omega.

A reprodução integral, traces independentes, ARM32/ARM64 e corpus completo continuam abertos.

## 6. Outputs P2 promovidos por evidência de identidade

`omega_hot_rank.txt`, `omega_metrics_abc.txt`, `omega_threads_rank.txt`, `zone_conv_streaks.txt` e os dois relatórios de exportação receberam:

- provider ID no Drive;
- pai `rmrCti`;
- tamanho;
- SHA-256;
- CRC32;
- gerador em revisão fixa do `llamaRafaelia`.

Os dois relatórios de exportação possuem bytes idênticos, mas provider IDs diferentes. Eles foram preservados como objetos distintos; deduplicação destrutiva permanece proibida.

`zone53.txt` está comprovadamente vazio por tamanho e hash, mas seu gerador e função continuam `TOKEN_VAZIO`.

## 7. Critério de evolução

Um caminho só pode sair de `VERIFIED_LIMITED_STATIC` quando houver, conforme o domínio:

1. entrada congelada e hash;
2. gerador em revisão exata;
3. execução reproduzível;
4. saída byte-idêntica ou tolerância declarada;
5. caso negativo;
6. receipt de ambiente;
7. revisão independente quando houver claim científico.

## R3

- **F_ok:** seis anchors lidos; sete outputs identificados e hashados; seis geradores ligados; 37 caminhos tipados governados pela IGC.
- **F_gap:** origem individualizada dos 94; regeneração dos outputs; `zone53.txt` sem gerador; Termux físico; revisão e métricas científicas independentes.
- **F_next:** executar a fatia `gerador → output → SHA-256` no ambiente de referência e depois no Termux, preservando resultados positivos e negativos em receipts sucessores.
