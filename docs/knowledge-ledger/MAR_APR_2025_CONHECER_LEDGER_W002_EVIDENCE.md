# RAFAELIA — Conhecer / Mar–Abr 2025 — Evidence Window W002

**State:** `DRAFT_EVIDENCE_BOUND`  
**Privacy:** public index only; raw private identifiers and verbatim source wording remain in Drive custody.

## Delta
- Four April objects were bound to the Locator by exact pseudonymized PID.
- One exact PID supports a same-conversation cluster joining Newton/classical physics, astronomy, light, knowing/learning, life and library/indexing semantics.
- Exact `luneta` and `convexo` were not observed in the scoped shard; `lente` is not promoted as same-conversation evidence.
- User method pattern is represented only as a paraphrase: deep inquiry → test/check/validate → iterate under stress → correlate domains → preserve access / avoid destructive loss.

## Gap update
- `CROSS_EXPORT_IDENTITY` -> closed only for four exact April PIDs.
- `SAME_CONVERSATION_OPTICS_CONHECER` -> narrowed to `SAME_CONVERSATION_LUNETA_LENTE_CONVEXO`.
- `MARCH_CONTENT_BINDING` -> remains `TOKEN_VAZIO`.

## R3
F_ok: exact April PID identity + scoped relation + method pattern.
F_gap: March fine binding; luneta/lente/convexo exact binding.
F_next: probe only authorized source-bearing Mar–Apr shards; otherwise preserve TOKEN_VAZIO.
