# RAFAELIA — Conhecer / Mar–Abr 2025 — W006

**State:** `CONTROLLED_CONTINUATION_PRIVACY_SAFE`  
**Parent:** `REC-MARAPR-CONHECER-W005-20260912`  
**Window:** 8 modules / max 3 active gap families  
**Claim:** `claim_allowed=false`

## Authority chain

`START HERE → private Drive custody → public alias → typed relation → gate → receipt → R3`

Invariants:

- `SOURCE != ARTIFACT != EXECUTION != EVIDENCE != CLAIM`
- `USER_INPUT != ASSISTANT_GENERATION != CLAIM`
- `TOKEN_VAZIO != 0`
- `SEARCH_MISS != GLOBAL_ABSENCE`
- correction is append/supersede; historical records are not erased.

## W006 delta

1. The eight-module route remains stable:
   `ROUTER / MARÇO / ABRIL / ÓPTICA-NEWTON-LUZ / CONHECER / BIBLIOTECA-INDEXAÇÃO / COORDENAÇÃO-VERSIONAMENTO / RECEIPTS`.
2. March later-month evidence remains role-typed and source-bound; assistant-generated optics/astronomy language is not promoted to user authorship.
3. April bounded evidence keeps Newton/classical physics/optics/light connected to knowledge-organization framing as a historical textual relation only.
4. The literal exact-source binding for `luneta` / `convexo` remains unresolved; controlled variants such as lens/telescope/refraction/optics/prism do not close that gap by themselves.
5. PR #607 is historical/closed; W006 is a successor and does not reopen or rewrite it.
6. Public repository remains alias-only and excludes raw private Drive, conversation and message identifiers.

## Active gaps — ceiling 3

1. `GAP:MARAPR:LENS_LUNETA_LITERAL_BINDING`
2. `GAP:MARAPR:MARCH_OPTICS_METHOD_BINDING`
3. `GAP:MARAPR:CROSS_EXPORT_IDENTITY`

## F_NEXT

Run one authoritative exact-source probe for either:

- `2025-03-01..13` with optics/Newton/method binding; or
- literal `luneta` / `convexo`.

If neither resolves, preserve `TOKEN_VAZIO`; do not create a fourth gap family.

## R3

`<F_ok: W006 continues W005 without regression; F_gap: 3 families; F_next: 1 exact authoritative probe>`

`raw_private_source_mutated=false`  
`append_only=true`  
`claim_allowed=false`
