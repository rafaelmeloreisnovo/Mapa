# Relation Calculus — Cross-Repository Route

**Date:** 2026-09-05  
**State:** `FEDERATED_ROUTE / CLAIM_ALLOWED=false`

## Purpose

This route records where each truth class lives. Mapa does not re-prove the mathematics and does not convert commits into scientific validation.

## Authority map

| Repository | Authority |
|---|---|
| `rafaelmeloreisnovo/Matem-tica-` | exact formulas, proofs, derivative/inverse/antiderivative calculus |
| `rafaelmeloreisnovo/papers` | research synthesis, heuristics, paper decomposition, hypotheses |
| `rafaelmeloreisnovo/ChipQuantum` | executable mathematical witnesses and regression tests |
| `instituto-Rafael/relativity-living-light` | RLL scientific integration and observational claim boundary |
| `rafaelmeloreisnovo/TeoremasTesesTeorias` | theorem catalog and prior-art/novelty gate |
| `rafaelmeloreisnovo/Mapa` | federated route, epistemic state and cross-repo relation registry |

Machine registry:

```text
data/ontology/relation_calculus_crossrepo.v1.json
```

## Core route

```text
geometric observation
-> formal object
-> direct relation
-> inverse relation
-> derivative/discrete difference
-> antiderivative/cumulative relation
-> exact theorem or bounded conjecture
-> executable witness
-> research hypothesis
-> RLL observational binding when applicable
-> prior-art review
-> independent reproduction
-> claim gate
```

## Current exact nodes

```text
T(c) = (1+3c)/(2(1+c))
z(T(c)) = z(c)/4
T^-1(y) = (2y-1)/(3-2y)
T'(c) = 1/(1+c)^2
integral T(c)dc = 3c/2 - ln|1+c| + C
R_n = sum_(k=0)^(n+1) F_k
Delta R_n = F_(n+1)
q_7 = (0,1,2,3,3,2,1)
kappa_7 = (2,0,0,-1,-1,0,0)
period(7,10,12,20) = 420
```

## Current correction node

The exact real-valued affine fixed point for

```text
q = sqrt(3)/2
b = -pi sin(279 degrees)
x* = b/(1-q)
```

is

```text
23.16046864479797...
```

Historical `23.158` is retained as `LEGACY_QUANTIZED_OR_ROUNDED` until the Q16 contract is reconstructed. History is not overwritten.

## RLL-specific direct/inverse pairs

```text
g(a) -> w_eff(a) via derivative
w_eff(a) -> g(a) via logarithmic antiderivative
H(z) -> D_L(z) via integral
D_L(z) -> H(z) via derivative, numerically ill-posed without regularization
CPL w(z) -> density factor X(z) via integral
```

These relations are classical/calculus-level bridges. Their presence does not validate the RLL physical hypothesis.

## Heuristic-only edges

```text
midpoint projective factor 1/4 -> possible renormalization kernel
modular period 420 -> candidate indexing architecture
sqrt(3)/2 reuse -> candidate parameter study
pi*phi/5 -> geometric arc/chord identity
```

None of these edges is a physical mechanism until an explicit input/output/observable/falsifier contract is present.

## Exclusive gates

```text
heuristic != proof
correlation != derivation
inverse != causal reversal
same scalar != same object
mathematical elegance != physical evidence
commit != execution
local test != independent reproduction
novel composition != novel constituent identity
```

## Open gaps

```text
TOKEN_VAZIO_PRIOR_ART_MIDPOINT_PROJECTIVE_PACKAGE
TOKEN_VAZIO_Q16_CANONICAL_RECONCILIATION_23_158
TOKEN_VAZIO_FREQUENCY_F_FACE_CLASSIFICATION
TOKEN_VAZIO_RULER_OPTIMALITY
TOKEN_VAZIO_RLL_OBSERVABLE_BINDING_FOR_GEOMETRIC_HEURISTICS
TOKEN_VAZIO_INDEPENDENT_REPRODUCTION
```

## F_next

1. Run exact ChipQuantum branch tests under provider CI and Android/Termux.
2. Perform primary-source prior-art review for the midpoint Möbius package.
3. Add symbolic/numeric KAT for RLL derivative identities.
4. Test RLL identifiability hypotheses against w0wa/CPL/GEDE/IDE models.
5. Generalize the f=2 face classification to frequency `f`.
6. Only after those gates, reassess novelty/publication status.
