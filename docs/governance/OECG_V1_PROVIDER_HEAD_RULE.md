# OECG V1 exact-head rule

Do not encode the candidate commit SHA inside a file whose commit would change that SHA. Resolve exact candidate head from GitHub provider readback and bind it in the successor execution receipt after tests.
