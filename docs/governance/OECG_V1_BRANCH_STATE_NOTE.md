# OECG V1 branch-state note

The branch advanced after PR #636 creation because the PR pointer itself was appended. Therefore the PR-creation head is historical only; execution evidence must bind the later exact head.

`HEAD_MOVED -> OLD_CI_NOT_SUFFICIENT_FOR_NEW_HEAD`
