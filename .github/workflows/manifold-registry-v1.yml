name: Manifold Registry V1
on:
  pull_request:
    paths:
      - 'schemas/manifold-*.json'
      - 'data/manifold/**'
      - 'tools/validate_manifold_registry.py'
      - 'tools/validate_manifold_gap_registry.py'
      - 'tools/resolve_manifold_route.py'
      - 'tools/resolve_manifold_gap.py'
      - 'tests/test_manifold*.py'
      - 'tests/fixtures/manifold_route_resolver_cases.json'
      - '.github/workflows/manifold-registry-v1.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: Validate registry and gap subgraph
        run: python tools/validate_manifold_registry.py data/manifold/edges_omega_v1.jsonl data/manifold/routes_omega_v1.jsonl data/manifold/gaps_omega_v1.jsonl
      - name: Validate recursive gap registry
        run: python tools/validate_manifold_gap_registry.py data/manifold/gaps_omega_v1.jsonl
      - name: Run manifold tests
        run: python -m unittest discover -s tests -p 'test_manifold*.py'
