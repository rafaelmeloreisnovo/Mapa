# Operational Excellence receipts

Receipts in this directory are immutable state observations for bounded governance work. A later correction appends a successor receipt with `parent`/`supersedes`; it does not edit the meaning of an earlier observation.

`IMPLEMENTED_UNTESTED != PASS`

`CI_PASS != GLOBAL_COMPLIANCE`

`TOKEN_VAZIO != 0`
