# Route — Voynich Implementation V1.1 / Three Pillars

## Purpose

Route the append-only V1.1 refinement without copying private bodies into the public map and without promoting scientific claims.

## Authority matrix

| Object | Authority | Path / pointer | State |
|---|---|---|---|
| executable V1.1 overlay | `Rafaelia_Private` | PR `#195`, `native/voynich_impl/` | draft, protected |
| mathematical consequences | `ChipQuantum` | PR `#48`, `docs/voynich/` | draft, protected |
| longitudinal report | Google Drive | document `1NpyxQtT85wlkzP0-Mu5mRd4llg0PhsjrGj0Lat3i834` | append-only update |
| project canonical memory | Google Drive | document `1g3eVD3zLMuwk0jevAwVL3wSmxhEMkKsAUPFQh2wEn88` | pointer only |
| route and dependency graph | `Mapa` | this file | mapping authority |

## Three pillars

```text
P1 SOURCE_PROVENANCE
P2 EXECUTION_REPLAY
P3 INTERPRETATION_CLAIM_BOUNDARY
```

Routing rule:

```text
2/3 pillars -> CHECKPOINT_CANDIDATE
3/3 pillars -> COHERENT_ENGINEERING_CHECKPOINT
3/3 + domain gates -> CLAIM_PROMOTION_ELIGIBLE
```

Current state:

```text
pillar_mask = 0b111
checkpoint = COHERENT_3_OF_3
claim_allowed = false
```

## Dependency chain

```text
Voynich V1 merged
  -> V1.1 append-only transformer
  -> portable C11 refined translation unit
  -> event ledger + replay
  -> three-pillar checkpoint
  -> ARMv7 Termux runtime receipt
  -> real Voynich corpus manifest
  -> real HRV manifest
  -> independent reproduction
  -> claim review
```

## Invariant

The invariant is preserved when every derived state retains a stable predecessor and a reproducible event:

```text
source -> hash -> event -> execution -> replay -> interpretation -> decision
```

No derived index is allowed to replace the source. Corrections are new events. `TOKEN_VAZIO` is retained as an auditable state.

## Current gaps

- `armv7_termux_runtime_verified`;
- `real_voynich_corpus_manifest_verified`;
- `real_hrv_dataset_manifest_verified`;
- `independent_reproduction`.

## Next route

The next receipt belongs first to `Rafaelia_Private` under the V1.1 audit path. After validation, `Mapa` receives only the commit, hashes, gate result and stable Drive pointer. Raw private data remains in its authority container.

> Ruído entendido vira sinal. Erro medido vira engenharia. Lacuna marcada vira ciência.
