# RLL Session Avalanche — federated route

```text
RLL physics @ pinned merge
  + RafGitTools ordered session capsule @ pinned merge
  -> RafPolimata finite permutation and scaling plan @ pinned merge
  -> Mapa pointer-only projection
```

The Mapa repository does not duplicate equations, session bodies or private
payloads. It records exact authorities, immutable external commit pins,
artifacts and typed edges.

“Physical avalanche” remains an RLL model candidate. “Computational
Avalanche” remains a RafPolimata fan-out policy. The map asserts no physical
equivalence.

```text
map_is_source_authority=false
private_payload_copied=false
automatic_cross_repo_write=false
automatic_merge=false
claim_allowed=false
```
